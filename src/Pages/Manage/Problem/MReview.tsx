import {withTranslation} from "react-i18next";
import {withRouter} from "react-router";

const MReview = (props: any) =>{
    return (
        <></>
    )
}

export default withTranslation()(withRouter(MReview))