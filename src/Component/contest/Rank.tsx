import {withTranslation} from "react-i18next";
import {withRouter} from "react-router-dom";

const Rank = (props: any) => {
    return (
        <>
            排行页面
        </>
    )
}

export default withTranslation()(withRouter(Rank))