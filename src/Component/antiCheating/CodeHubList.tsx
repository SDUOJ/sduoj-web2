import {withTranslation} from "react-i18next";
import TableWithSelection from "../common/Table/TableWithSelection";

const CodeHubList = (props: any) =>{
    return (
        <>
            代码仓库列表
        </>
    )
}

export default withTranslation()(CodeHubList)